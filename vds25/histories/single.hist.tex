\documentclass[tikz,convert=pdf2svg]{standalone}
         \input figure_macros
         \usepackage{timeline}
         \usepackage{xcolor}
         \directlua{texh = require('texhist')}
         \begin{document}
         \begin{timeline}
         \directlua{tex.print(texh.h2t('single.hist', false))}
         \end{timeline}
         \end{document}
